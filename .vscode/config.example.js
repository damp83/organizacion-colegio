// Copia este archivo a config.js y rellena con tus credenciales de Firebase.
// Este archivo define las variables globales que espera app.js.

// Identificador de la app (puede ser cualquier string para separar datos por app en Firestore)
window.__app_id = 'mi-centro-educativo';

// Config de Firebase. Puedes dejarlo como objeto o como string JSON.
window.__firebase_config = {
  apiKey: "TU_API_KEY",
  authDomain: "tu-proyecto.firebaseapp.com",
  projectId: "tu-proyecto",
  storageBucket: "tu-proyecto.appspot.com",
  messagingSenderId: "000000000000",
  appId: "1:000000000000:web:xxxxxxxxxxxxxxxx",
};

// Si usas autenticación con token personalizado, define el token aquí; si no, deja undefined.
// window.__initial_auth_token = "TOKEN_JWT";
